interface:
  display_name: "EIAN Collage B-roll"
  short_description: "输入路由→隐喻确认→尾帧确认→首尾帧与视频提示词素材包"
  default_prompt: "Use $eian-collage-broll 按 SKILL.md 的输入路由、Gate 1 / Gate 2 确认与逐 item 推进规则，把口播制作为 first-frame.png、last-frame.png 和 video-prompt.txt 素材包，并按正式规则完成 Project Cleanup。"
policy:
  allow_implicit_invocation: true
